# Setutu API

## Local development

```bash

    # git clone
    git clone https://github.com/guiyumin/setutu-api.git
    # go to the directory
    cd setutu-api
    # setup env vars
    cp .env.example .env
    # run it locally
    make dev
```
